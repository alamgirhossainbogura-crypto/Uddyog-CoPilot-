name: Build & Verify Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Verify Setup
        run: echo "Uddyog-CoPilot Build Pipeline Triggered Successfully!"
